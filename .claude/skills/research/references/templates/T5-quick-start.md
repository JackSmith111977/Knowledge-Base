# T5 快速入门模板

> **用途**：5 分钟上手指南 / 速查卡 / 快速参考
> **适用场景**：新技术入门、API 速查、工具快速上手
> **目标**：读者 5 分钟内能跑通第一个示例

---

## 一句话简介

> **{技术名称} 是什么**：一句话定义
> **为什么用它**：解决什么痛点 / 核心优势

---

## 环境要求

| 依赖 | 最低版本 | 说明 |
|------|----------|------|
| Node.js | x.x+ | 如适用 |
| Python | x.x+ | 如适用 |
| 其他 | - | 如 API Key、数据库等 |

---

## 安装

```bash
# 一条命令完成安装
{install-command}
```

---

## Hello World

```{language}
// 最小可运行示例 — 复制粘贴即可运行
{code}
```

**预期输出**：`{expected-output}`

---

## 核心 API 速览

| API / 方法 | 用途 | 参数 | 返回值 | 示例 |
|------------|------|------|--------|------|
| `name()` | 做什么 | `(type)` | `type` | `name(arg)` |
| `name()` | 做什么 | `(type)` | `type` | `name(arg)` |
| `name()` | 做什么 | `(type)` | `type` | `name(arg)` |

> 只列最常用的 5-8 个，完整 API 见官方文档。

---

## 常见坑

| ❌ 错误做法 | ✅ 正确做法 | 原因 |
|-------------|-------------|------|
| `{wrong-code}` | `{right-code}` | 简要说明 |
| `{wrong-code}` | `{right-code}` | 简要说明 |
| `{wrong-code}` | `{right-code}` | 简要说明 |

---

## 下一步

- **深入理解** → 阅读 T1 详细文档：`{t1-doc-link}`
- **架构设计** → 查看 T4 架构参考：`{t4-doc-link}`
- **完整案例** → 参考 T8 实战教程：`{t8-doc-link}`
- **官方文档**：{official-doc-url}
