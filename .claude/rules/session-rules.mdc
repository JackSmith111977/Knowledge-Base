# 会话规则与行为准则

> 适用：所有 Claude Code 会话

---

## 核心规则

### 搜索规范

- **禁止使用原生 WebSearch**：必须使用 MCP 提供的 `mcp__WebSearch__bailian_web_search` 进行搜索
- WebFetch 已配置 `skipWebFetchPreflight: true`，无需确认

### 工具使用规范

- 优先使用专用工具（Read/Edit/Write/Glob/Grep）而非 Bash 命令
- 仅在需要执行系统命令时使用 Bash

### 沟通风格

- 响应简洁直接，避免冗长前言
- 使用表格、列表等结构化格式输出
- 代码变更使用 Diff 格式展示

---

## 文件操作规范

### 读写顺序

- 修改文件前必须先读取
- 编辑时使用精确的字符串匹配

### 路径规范

- 使用绝对路径而非相对路径
- Windows 路径使用正斜杠 `/` 或转义反斜杠 `\\`

---

## 任务执行规范

### 多任务处理

- 独立任务并行执行
- 依赖任务顺序执行

### 进度追踪

- 复杂任务使用 `progress.txt` 追踪进度
- 任务完成后及时更新状态

---

## 知识库规范

- 文档存储位置优先查询 KB-INDEX.md
- 新技术主题添加到索引
- 过程文件统一存放到 `.work/[主题]/drafts/`
